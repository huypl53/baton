# Go Gin Starter

A minimal Go REST API using the Gin framework.

## Run

```bash
go run main.go
```

## Endpoints

- `GET /users` - List all users
- `GET /users/:id` - Get user by ID
- `POST /users` - Create new user
